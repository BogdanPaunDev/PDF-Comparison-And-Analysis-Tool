[project]
name = "pdf-comparison-tool"
version = "1.0.0"
description = "A CLI tool for comparing and analyzing PDF files."
requires-python = ">=3.10"

dependencies = [
    "pypdf"
]

[project.scripts]
compare = "pdf_compare.main:main"